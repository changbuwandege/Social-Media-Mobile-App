import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    title:{
        fontFamily: '宋体',
        fontWeight: '900',
        fontSize: 24,
        lineHeight: 29,
        color: 'black',
    }
});

export default style;